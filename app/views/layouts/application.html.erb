<!DOCTYPE html>
<html>
<head>
  <title><%= controller_name.humanize.titleize %>: <%= controller.action_name %></title>

  <%= javascript_importmap_tags %>
  <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
  <%= csrf_meta_tag %>
</head>
<body>

  <nav class="bg-blue-500 text-white p-2 flex justify-between items-center">
    <a class="text-xl font-bold" href="/">Alpkem</a>
    <ul class="nav navbar-nav flex gap-2">
        <li class="mr-2"><%= link_to 'Samples', samples_path %></li>
      <% if user_signed_in? %>
        <li><%= link_to 'Sign Out', destroy_user_session_path, data: { turbo_method: :delete } %></li>
      <% else %>
        <li><%= link_to 'Sign In', new_user_session_path  %></li>
      <% end %>
    </ul>
  </nav>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>

<div class="container mx-auto px-4 py-8">
  <%= yield %>
</div>

<%= debug(params) if Rails.env.development? %>

</body>
</html>
