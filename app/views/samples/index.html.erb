<%= paginate @samples%>

<%= form_tag("/samples/search", method: "get", html: {class: "form"}) do %>
    <%= text_field_tag(:q, @q , {placeholder: "Search for Plots", class: "form-control"}) %>
    <%= submit_tag("Search", {class: "btn btn-primary", data: { disable_with: "Please wait..." } }) %>
<% end %>

<table class='table table-striped table-hover'>
  <tr>
    <th>Date</th>
    <th>Experiment</th>
    <th>Sample Type</th>
    <th>Plot</th>
    <th>Compound</th>
    <th>Conc.</th>
    <th>Compound</th>
    <th>Conc.</th>
  </tr>
  <% @samples.each do |sample| %>
    <%= render partial: sample, locals: {sample: sample}%>
  <% end %>
</table>

<%= paginate @samples%>
