<% cv = sample.cv(analyte) %>
<td class="cv_high_<%= cv > 10 || cv < 0 %>"  id="sample_<%= sample.id %>_<%= analyte.id %>">
  <table>
    <% relevant_measurements = @measurements.collect {|measurement| measurement if measurement.sample == sample && measurement.analyte == analyte}.compact %>
    <% relevant_measurements.each do |measurement| %>
      <% number = number_with_precision(measurement.amount,:precision => 4) %>
      <tr>
        <td>
          <% if sample.approved? %>
            <span class="deleted_<%=measurement.deleted?%>"><%= number %></span>
          <% else %>
            <%= link_to "#{number}", 
                 {:controller => 'measurements',
                  :action => 'destroy',
                  :id => measurement.id,
                  :sample_class => sample.class,
                  :run_id => @run.id},
                 :method => 'delete',
                 :class => "deleted_#{measurement.deleted?}",
                :remote => true %>
          <% end %>
        </td>
      </tr>
    <% end %>
    <% relevant_measurements.keep_if {|measurement| measurement.deleted == false} %>
    <tr>
      <td>Mean:</td>
      <td><%= number_with_precision(average(relevant_measurements), :precision => 4) %></td>
    </tr>
    <tr>
      <td>CV:</td>
      <td><%= number_with_precision(cv, :precision=>4) %></td>
    </tr>
  </table>
</td>
