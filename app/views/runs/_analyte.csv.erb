<% relevant_measurements = sample.measurements.where(analyte_id: analyte.id).all.compact %>
<% stat_measurements = relevant_measurements.collect {|measurement| measurement if measurement.deleted == false}.compact -%>
<% cv = RunsHelper.cv_help(stat_measurements) -%>
<%= number_with_precision(RunsHelper.average(stat_measurements), :precision => 4) %>
